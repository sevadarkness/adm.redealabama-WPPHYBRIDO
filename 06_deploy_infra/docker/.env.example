# .env para uso com docker compose (06_deploy_infra/docker)
#
# Este arquivo é usado em 2 lugares:
# 1) docker compose (substituição de variáveis + env_file)
# 2) Montado dentro do container em /var/www/html/01_backend_painel_php/.env
#    para que o backend PHP carregue as configs.

APP_ENV=local
APP_DEBUG=true

# Banco (docker compose)
DB_HOST=db
DB_PORT=3306
DB_DATABASE=redealabama
DB_USERNAME=redealabama
DB_PASSWORD=redealabama123

# (opcional) senha do root do MariaDB
DB_ROOT_PASSWORD=root

APP_NAME="Rede Alabama – Painel Administrativo"
APP_URL="http://localhost:8000"

SENTRY_DSN=

# Configuração de IA / LLM
LLM_PROVIDER=openai
LLM_TIMEOUT_SECONDS=15

# Chave da OpenAI:
# - Recomendado: OPENAI_API_KEY
# - Alternativas/compat: ALABAMA_OPENAI_API_KEY ou LLM_OPENAI_API_KEY
OPENAI_API_KEY=
ALABAMA_OPENAI_API_KEY=
LLM_OPENAI_API_KEY=

# Tokens auxiliares (opcionais)
ALABAMA_APPLY_ENV_TOKEN=